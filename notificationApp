from plyer import notification
import time

if __name__ == '__main__' :
    while True:
        notification.notify(
            title="*** Rest Time ***",
            message = "Rest is vital for mental health",
            timeout=5)
        time.sleep(10)
